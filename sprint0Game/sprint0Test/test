using Microsoft.Xna.Framework.Graphics;
using Microsoft.Xna.Framework;
using System;
using System.Text;
using System.Collections.Generic;

namespace sprint0Test.Sprites
{

    class StandingInPlacePlayerSprite : ISprite
    {
        //private int currentFrame = 0;
        private Rectangle destination;
        private Texture2D texture;
        private Rectangle sorceRect = new Rectangle(45, 500, 20, 20);
        public int LocationX { get; set; }
        public int LocationY { get; set; }
        public StandingInPlacePlayerSprite (Texture2D texture, Vector2 location)
        {
            this.texture = texture;
        }
        public void Draw(SpriteBatch spriteBatch, Texture2D spritesheet)
        {
                //spriteBatch.Draw(spritesheet, destination, sorceRect, Color.AntiqueWhite);
        }
        public Rectangle GetSpriteRect()
        {
            return destination;
        }

        public void Update()
        {
            //currentFrame++;
            //if (currentFrame == totalFrames)
               // currentFrame = 0;
        }
    }
}